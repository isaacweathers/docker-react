language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t isaacweathersnet/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true isaacweathersnet/docker-react npm run test